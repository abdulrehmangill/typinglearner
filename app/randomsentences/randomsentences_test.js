'use strict';

describe('myApp.randomsentences module', function() {

  beforeEach(module('myApp.randomsentences'));

  describe('randomsentences controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var randomsentencesCtrl = $controller('randomsentencesCtrl');
      expect(randomsentencesCtrl).toBeDefined();
    }));

  });
});