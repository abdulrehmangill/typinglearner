'use strict';

describe('myApp.wordtoword module', function() {

  beforeEach(module('myApp.wordtoword'));

  describe('wordtoword controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var wordtowordCtrl = $controller('wordtowordCtrl');
      expect(wordtowordCtrl).toBeDefined();
    }));

  });
});